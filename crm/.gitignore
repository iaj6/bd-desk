node_modules/
.next/
.vercel
.env*
!.env.example
tsconfig.tsbuildinfo

# regenerated by `next dev`/`next build` on every run (dev and build write
# different import paths, so a tracked copy is dirty after `npm run demo`)
next-env.d.ts

# local demo pipeline (npm run demo)
.demo-data/

# written by `next dev` on every run (Next.js agent instruction files)
AGENTS.md
CLAUDE.md
