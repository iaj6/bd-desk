## What this changes

<!-- One or two sentences. If it fixes an issue, link it. -->

## Why

<!-- Especially valuable if this encodes a platform behaviour you had to work out —
     consider adding it to the README's "Hard-won platform notes" too. -->

## Checks

- [ ] `npm run check` passes at the repo root (lint + typecheck + tests)
- [ ] `cd crm && npm run lint && npm run typecheck && npm run build` passes
- [ ] `cd crm && npm run demo` still boots with no credentials
- [ ] No automated path can send anything without a human
- [ ] No secrets, real company names, or private positioning in the diff
